// Stampd Dashboard
// Shop owner view — customer list, stamp stats, campaign sender

export default function DashboardPage() {
  return (
    <main>
      {/* TODO: implement dashboard — customer table, analytics, campaign sender */}
      <p>Dashboard — coming in V2 (Month 3–6 per roadmap)</p>
    </main>
  )
}
